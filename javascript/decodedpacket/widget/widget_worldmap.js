DecodedPacketWidget.prototype.widget_595 = function(player, index, childID, slot, itemID) {
    switch (childID) {
    case 3:
    case 37:
        player.getWidgetManager().removeWorldMap();
        break;
    }
}
