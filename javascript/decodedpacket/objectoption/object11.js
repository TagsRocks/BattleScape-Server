/* 2816 - 3071 */

// Rock of Dalgroth
DecodedPacketObjectOption.prototype.object_2816 = function(player, index, object) { }

// Cave exit
DecodedPacketObjectOption.prototype.object_2817 = function(player, index, object) { }

// Cave exit
DecodedPacketObjectOption.prototype.object_2818 = function(player, index, object) { }

// Cave exit
DecodedPacketObjectOption.prototype.object_2819 = function(player, index, object) { }

// Cave exit
DecodedPacketObjectOption.prototype.object_2820 = function(player, index, object) { }

// Cave exit
DecodedPacketObjectOption.prototype.object_2821 = function(player, index, object) { }

// Cave exit
DecodedPacketObjectOption.prototype.object_2822 = function(player, index, object) { }

// Hole
DecodedPacketObjectOption.prototype.object_2823 = function(player, index, object) { }

// Climbing rope
DecodedPacketObjectOption.prototype.object_2825 = function(player, index, object) { }

// Chest
DecodedPacketObjectOption.prototype.object_2826 = function(player, index, object) { }

// Chest
DecodedPacketObjectOption.prototype.object_2827 = function(player, index, object) { }

// null
DecodedPacketObjectOption.prototype.object_2828 = function(player, index, object) { }

// null
DecodedPacketObjectOption.prototype.object_2829 = function(player, index, object) { }

// Gap
DecodedPacketObjectOption.prototype.object_2830 = function(player, index, object) { }

// Gap
DecodedPacketObjectOption.prototype.object_2831 = function(player, index, object) { }

// Battlement
DecodedPacketObjectOption.prototype.object_2832 = function(player, index, object) { }

// Ladder
DecodedPacketObjectOption.prototype.object_2833 = function(player, index, object) { }

// Battlement
DecodedPacketObjectOption.prototype.object_2834 = function(player, index, object) { }

// Rock
DecodedPacketObjectOption.prototype.object_2835 = function(player, index, object) { }

// Rock
DecodedPacketObjectOption.prototype.object_2836 = function(player, index, object) { }

// Rock
DecodedPacketObjectOption.prototype.object_2837 = function(player, index, object) { }

// Drain
DecodedPacketObjectOption.prototype.object_2843 = function(player, index, object) { }

// Sewer valve
DecodedPacketObjectOption.prototype.object_2844 = function(player, index, object) { }

// Sewer valve
DecodedPacketObjectOption.prototype.object_2845 = function(player, index, object) { }

// Sewer valve
DecodedPacketObjectOption.prototype.object_2846 = function(player, index, object) { }

// Sewer valve
DecodedPacketObjectOption.prototype.object_2847 = function(player, index, object) { }

// Sewer valve
DecodedPacketObjectOption.prototype.object_2848 = function(player, index, object) { }

// Raft
DecodedPacketObjectOption.prototype.object_2849 = function(player, index, object) { }

// Cupboard
DecodedPacketObjectOption.prototype.object_2850 = function(player, index, object) { }

// Cupboard
DecodedPacketObjectOption.prototype.object_2851 = function(player, index, object) { }

// Cave entrance
DecodedPacketObjectOption.prototype.object_2852 = function(player, index, object) { }

// Stairs
DecodedPacketObjectOption.prototype.object_2853 = function(player, index, object) { }

// Wall
DecodedPacketObjectOption.prototype.object_2854 = function(player, index, object) { }

// Chest
DecodedPacketObjectOption.prototype.object_2856 = function(player, index, object) { }

// Chest
DecodedPacketObjectOption.prototype.object_2857 = function(player, index, object) { }

// Crate
DecodedPacketObjectOption.prototype.object_2858 = function(player, index, object) { }

// Door
DecodedPacketObjectOption.prototype.object_2861 = function(player, index, object) { }

// Door
DecodedPacketObjectOption.prototype.object_2862 = function(player, index, object) { }

// Door
DecodedPacketObjectOption.prototype.object_2863 = function(player, index, object) { }

// Fountain
DecodedPacketObjectOption.prototype.object_2864 = function(player, index, object) { }

// Gate
DecodedPacketObjectOption.prototype.object_2865 = function(player, index, object) { }

// Gate
DecodedPacketObjectOption.prototype.object_2866 = function(player, index, object) { }

// Potted plant
DecodedPacketObjectOption.prototype.object_2867 = function(player, index, object) { }

// Cupboard
DecodedPacketObjectOption.prototype.object_2868 = function(player, index, object) { }

// Cupboard
DecodedPacketObjectOption.prototype.object_2869 = function(player, index, object) { }

// Ladder
DecodedPacketObjectOption.prototype.object_2871 = function(player, index, object) { }

// Ladder
DecodedPacketObjectOption.prototype.object_2872 = function(player, index, object) { }

// Statue of Saradomin
DecodedPacketObjectOption.prototype.object_2873 = function(player, index, object) { }

// Statue of Zamorak
DecodedPacketObjectOption.prototype.object_2874 = function(player, index, object) { }

// Statue of Guthix
DecodedPacketObjectOption.prototype.object_2875 = function(player, index, object) { }

// Sparkling pool
DecodedPacketObjectOption.prototype.object_2878 = function(player, index, object) { }

// Sparkling pool
DecodedPacketObjectOption.prototype.object_2879 = function(player, index, object) { }

// Mystic portal
DecodedPacketObjectOption.prototype.object_2880 = function(player, index, object) { }

// Prison Door
DecodedPacketObjectOption.prototype.object_2881 = function(player, index, object) { }

// Gate
DecodedPacketObjectOption.prototype.object_2882 = function(player, index, object) { }

// Gate
DecodedPacketObjectOption.prototype.object_2883 = function(player, index, object) { }

// Ladder
DecodedPacketObjectOption.prototype.object_2884 = function(player, index, object) { }

// Cupboard
DecodedPacketObjectOption.prototype.object_2885 = function(player, index, object) { }

// Open Cupboard
DecodedPacketObjectOption.prototype.object_2886 = function(player, index, object) { }

// Jungle Tree
DecodedPacketObjectOption.prototype.object_2887 = function(player, index, object) { }

// Jungle Tree
DecodedPacketObjectOption.prototype.object_2889 = function(player, index, object) { }

// Jungle Tree
DecodedPacketObjectOption.prototype.object_2890 = function(player, index, object) { }

// Jungle Bush
DecodedPacketObjectOption.prototype.object_2892 = function(player, index, object) { }

// Jungle Bush
DecodedPacketObjectOption.prototype.object_2893 = function(player, index, object) { }

// Legends Guild door
DecodedPacketObjectOption.prototype.object_2896 = function(player, index, object) {
    Region.openDoors(player, object);
}

// Legends Guild door
DecodedPacketObjectOption.prototype.object_2897 = function(player, index, object) {
    Region.openDoors(player, object);
}

// Mossy rock
DecodedPacketObjectOption.prototype.object_2900 = function(player, index, object) { }

// Mossy rock
DecodedPacketObjectOption.prototype.object_2901 = function(player, index, object) { }

// Rocks
DecodedPacketObjectOption.prototype.object_2902 = function(player, index, object) { }

// Cave entrance
DecodedPacketObjectOption.prototype.object_2903 = function(player, index, object) { }

// Cave entrance
DecodedPacketObjectOption.prototype.object_2904 = function(player, index, object) { }

// Crate
DecodedPacketObjectOption.prototype.object_2905 = function(player, index, object) { }

// Table
DecodedPacketObjectOption.prototype.object_2906 = function(player, index, object) { }

// Bed
DecodedPacketObjectOption.prototype.object_2907 = function(player, index, object) { }

// Fire Wall
DecodedPacketObjectOption.prototype.object_2908 = function(player, index, object) {
    if (!player.carryingItem(730)) {
        player.getGameEncoder().sendMessage("You need a Binding book to enter.");
        return;
    }
    player.getMovement().teleport(2792, 9328, 0);
}

// Fire Wall
DecodedPacketObjectOption.prototype.object_2909 = function(player, index, object) {
    if (!player.carryingItem(730)) {
        player.getGameEncoder().sendMessage("You need a Binding book to enter.");
        return;
    }
    player.getMovement().teleport(2792, 9328, 0);
}

// Desk
DecodedPacketObjectOption.prototype.object_2910 = function(player, index, object) { }

// Bookcase
DecodedPacketObjectOption.prototype.object_2911 = function(player, index, object) {
    if (player.getSkills().getLevel(Skills.AGILITY) < 50) {
        player.getGameEncoder().sendMessage("You need an Agility level of 50 to do this.");
        return;
    }
    player.getMovement().ladderDownTeleport(new Tile(2800, 9340, 0));
    AchievementDiary.agilityObstacleHooks(player, object);
}

// Ancient Gate
DecodedPacketObjectOption.prototype.object_2912 = function(player, index, object) {
    if (object.getOriginal() != null || object.getAttachment() != null) {
        return;
    }
    if (player.getY() >= 9332) {
        player.getGameEncoder().sendMessage("You attempt to pick the lock.");
        if (!player.getInventory().hasItem(1523)) {
            player.getGameEncoder().sendMessage("You need a lockpick for this lock.");
            return;
        } else if (Utils.randomE(4) != 0) {
            player.getGameEncoder().sendMessage("You fail to pick the lock.");
            return;
        }
        player.getGameEncoder().sendMessage("You manage to pick the lock.");
        player.getMovement().clear();
        player.getMovement().addMovement(player.getX(), player.getY() - 1);
        Region.openDoors(player, object, 1, false);
    } else {
        player.getMovement().clear();
        player.getMovement().addMovement(player.getX(), player.getY() + 1);
        Region.openDoors(player, object, 1, false);
    }
}

// Ancient Gate
DecodedPacketObjectOption.prototype.object_2913 = function(player, index, object) {
    if (object.getOriginal() != null || object.getAttachment() != null) {
        return;
    }
    if (player.getY() >= 9332) {
        player.getGameEncoder().sendMessage("You attempt to pick the lock.");
        if (!player.getInventory().hasItem(1523)) {
            player.getGameEncoder().sendMessage("You need a lockpick for this lock.");
            return;
        } else if (Utils.randomE(4) != 0) {
            player.getGameEncoder().sendMessage("You fail to pick the lock.");
            return;
        }
        player.getGameEncoder().sendMessage("You manage to pick the lock.");
        player.getMovement().clear();
        player.getMovement().addMovement(player.getX(), player.getY() - 1);
        Region.openDoors(player, object, 1, false);
    } else {
        player.getMovement().clear();
        player.getMovement().addMovement(player.getX(), player.getY() + 1);
        Region.openDoors(player, object, 1, false);
    }
}

// Crevice
DecodedPacketObjectOption.prototype.object_2918 = function(player, index, object) {
    player.getMovement().ladderDownTeleport(new Tile(2795, 9338, 0));
}

// Boulder
DecodedPacketObjectOption.prototype.object_2919 = function(player, index, object) {
    if (player.getY() >= 9329) {
        player.getMovement().teleport(player.getX(), 9326, 0);
    } else {
        player.getMovement().teleport(player.getX(), 9329, 0);
    }
}

// Boulder
DecodedPacketObjectOption.prototype.object_2920 = function(player, index, object) {
    if (player.getY() >= 9325) {
        player.getMovement().teleport(player.getX(), 9322, 0);
    } else {
        player.getMovement().teleport(player.getX(), 9325, 0);
    }
}

// Boulder
DecodedPacketObjectOption.prototype.object_2921 = function(player, index, object) {
    if (player.getY() >= 9321) {
        player.getMovement().teleport(player.getX(), 9318, 0);
    } else {
        player.getMovement().teleport(player.getX(), 9321, 0);
    }
}

// Ancient Gate
DecodedPacketObjectOption.prototype.object_2922 = function(player, index, object) {
    if (object.getOriginal() != null || object.getAttachment() != null) {
        return;
    } else if (player.getSkills().getLevel(Skills.STRENGTH) < 50) {
        player.getGameEncoder().sendMessage("You need a Strength level of 50 to do this.");
        return;
    }
    player.getMovement().clear();
    if (player.getY() >= 9314) {
        player.getMovement().addMovement(player.getX(), player.getY() - 1);
    } else {
        player.getMovement().addMovement(player.getX(), player.getY() + 1);
    }
    Region.openDoors(player, object, 1, false);
}

// Ancient Gate
DecodedPacketObjectOption.prototype.object_2923 = function(player, index, object) {
    if (object.getOriginal() != null || object.getAttachment() != null) {
        return;
    } else if (player.getSkills().getLevel(Skills.STRENGTH) < 50) {
        player.getGameEncoder().sendMessage("You need a Strength level of 50 to do this.");
        return;
    }
    player.getMovement().clear();
    if (player.getY() >= 9314) {
        player.getMovement().addMovement(player.getX(), player.getY() - 1);
    } else {
        player.getMovement().addMovement(player.getX(), player.getY() + 1);
    }
    Region.openDoors(player, object, 1, false);
}

// Jagged wall
DecodedPacketObjectOption.prototype.object_2926 = function(player, index, object) {
    if (player.getX() == 2790 && player.getY() == 9295) {
        player.getMovement().teleport(2789, 9296, 0);
    } else if (player.getX() == 2789 && player.getY() == 9296) {
        player.getMovement().teleport(2790, 9295, 0);
    }
}

// Marked wall
DecodedPacketObjectOption.prototype.object_2927 = function(player, index, object) {
    if (object.getX() == 2779 && object.getY() == 9305) {
        if (player.getCombat().getLegendsQuest() == 0) {
            if (player.getInventory().getID(0) != 566 || player.getInventory().getID(1) != 558
                    || player.getInventory().getID(2) != 557 || player.getInventory().getID(3) != 563
                    || player.getInventory().getAmount(3) < 2) {
                player.getGameEncoder().sendMessage("You need to carry the SMELL runes to pass.");
                return;
            }
            player.getInventory().deleteItem(566, 1);
            player.getInventory().deleteItem(558, 1);
            player.getInventory().deleteItem(557, 1);
            player.getInventory().deleteItem(563, 2);
        }
        player.getController().addMapItem(new Item(730, 1), new Tile(2775, 9290, 0), player);
        player.getGameEncoder().sendMessage("You appear in a large cavern like room filled with pools of water.");
        player.getMovement().teleport(2773, 9301, 0);
    } else if (object.getX() == 2776 && object.getY() == 9303) {
        player.getMovement().teleport(2780, 9307, 0);
    }
}

// Carved rock
DecodedPacketObjectOption.prototype.object_2928 = function(player, index, object) { }

// Buried skeleton
DecodedPacketObjectOption.prototype.object_2929 = function(player, index, object) { }

// Ancient Gate
DecodedPacketObjectOption.prototype.object_2930 = function(player, index, object) {
    if (player.getCombat().getLegendsQuest() == 0) {
        player.getGameEncoder().sendMessage("I don't think I should go in there...");
        return;
    }
    player.getMovement().teleport(2762, 9320, 0);
}

// Barrel
DecodedPacketObjectOption.prototype.object_2932 = function(player, index, object) { }

// Winch
DecodedPacketObjectOption.prototype.object_2934 = function(player, index, object) {
    player.getGameEncoder().sendMessage("I need to find a way to climb down.");
}

// Winch
DecodedPacketObjectOption.prototype.object_2935 = function(player, index, object) { }

// Totem Pole
DecodedPacketObjectOption.prototype.object_2936 = function(player, index, object) { }

// Totem Pole
DecodedPacketObjectOption.prototype.object_2937 = function(player, index, object) { }

// Totem Pole
DecodedPacketObjectOption.prototype.object_2938 = function(player, index, object) { }

// Totem Pole
DecodedPacketObjectOption.prototype.object_2939 = function(player, index, object) { }

// Sacred water
DecodedPacketObjectOption.prototype.object_2941 = function(player, index, object) { }

// Water Pool
DecodedPacketObjectOption.prototype.object_2942 = function(player, index, object) { }

// Polluted water
DecodedPacketObjectOption.prototype.object_2943 = function(player, index, object) { }

// Tall Reeds
DecodedPacketObjectOption.prototype.object_2944 = function(player, index, object) { }

// Totem pole
DecodedPacketObjectOption.prototype.object_2954 = function(player, index, object) { }

// Climbing rope
DecodedPacketObjectOption.prototype.object_2958 = function(player, index, object) { }

// Rocky Ledge
DecodedPacketObjectOption.prototype.object_2959 = function(player, index, object) {
    if (object.getX() == 2377 && object.getY() == 4717) {
        if (player.getX() != 2378 || player.getY() != 4717) {
            return;
        }
        player.setAnimation(840);
        var toTile = new Tile(2376, 4717, 0);
        player.setForceMovement(new ForceMovement(player, 0, toTile, 2, Tile.WEST));
        player.lock();
        player.getGameEncoder().sendMessage("You start to climb the precarious rocks.");
        var event = new Event() {
            execute: function() {
                event.stop();
                if (!player.isVisible()) {
                    return;
                }
                player.unlock();
                player.getMovement().teleport(toTile);
                player.getGameEncoder().sendMessage("You climb confidently over the rocks and hold your balance well.");
            }
        };
        player.getWorld().addEvent(event);
    }
}

// Rocky Ledge
DecodedPacketObjectOption.prototype.object_2960 = function(player, index, object) {
    if (object.getX() == 2377 && object.getY() == 4728) {
        if (player.getX() != 2377 || player.getY() != 4727) {
            return;
        }
        player.setAnimation(840);
        var toTile = new Tile(2378, 4729, 0);
        player.setForceMovement(new ForceMovement(player, 0, toTile, 2, Tile.NORTH));
        player.lock();
        player.getGameEncoder().sendMessage("You start to climb the precarious rocks.");
        var event = new Event() {
            execute: function() {
                event.stop();
                if (!player.isVisible()) {
                    return;
                }
                player.unlock();
                player.getMovement().teleport(toTile);
                player.getGameEncoder().sendMessage("You climb confidently over the rocks and hold your balance well.");
            }
        };
        player.getWorld().addEvent(event);
    }
}

// Rocky Ledge
DecodedPacketObjectOption.prototype.object_2961 = function(player, index, object) {
    if (object.getX() == 2382 && object.getY() == 4729) {
        if (player.getX() != 2382 || player.getY() != 4730) {
            return;
        }
        player.setAnimation(840);
        var toTile = new Tile(2382, 4728, 0);
        player.setForceMovement(new ForceMovement(player, 0, toTile, 2, Tile.SOUTH));
        player.lock();
        player.getGameEncoder().sendMessage("You start to climb the precarious rocks.");
        var event = new Event() {
            execute: function() {
                event.stop();
                if (!player.isVisible()) {
                    return;
                }
                player.unlock();
                player.getMovement().teleport(toTile);
                player.getGameEncoder().sendMessage("You climb confidently over the rocks and hold your balance well.");
            }
        };
        player.getWorld().addEvent(event);
    }
}

// Rocks
DecodedPacketObjectOption.prototype.object_2962 = function(player, index, object) {
    if (object.getX() == 2387 && object.getY() == 4728) {
        if (player.getX() != 2387 || player.getY() != 4727) {
            return;
        }
        player.setAnimation(840);
        var toTile = new Tile(2388, 4729, 0);
        player.setForceMovement(new ForceMovement(player, 0, toTile, 2, Tile.NORTH));
        player.lock();
        player.getGameEncoder().sendMessage("You start to climb the precarious rocks.");
        var event = new Event() {
            execute: function() {
                event.stop();
                if (!player.isVisible()) {
                    return;
                }
                player.unlock();
                player.getMovement().teleport(toTile);
                player.getGameEncoder().sendMessage("You climb confidently over the rocks and hold your balance well.");
            }
        };
        player.getWorld().addEvent(event);
    }
}

// Rocks
DecodedPacketObjectOption.prototype.object_2963 = function(player, index, object) {
    if (object.getX() == 2390 && object.getY() == 4724) {
        if (player.getX() != 2390 || player.getY() != 4725) {
            return;
        }
        player.setAnimation(840);
        var toTile = new Tile(2390, 4723, 0);
        player.setForceMovement(new ForceMovement(player, 0, toTile, 2, Tile.SOUTH));
        player.lock();
        player.getGameEncoder().sendMessage("You start to climb the precarious rocks.");
        var event = new Event() {
            execute: function() {
                event.stop();
                if (!player.isVisible()) {
                    return;
                }
                player.unlock();
                player.getMovement().teleport(toTile);
                player.getGameEncoder().sendMessage("You climb confidently over the rocks and hold your balance well.");
            }
        };
        player.getWorld().addEvent(event);
    }
}

// Rocks
DecodedPacketObjectOption.prototype.object_2964 = function(player, index, object) {
    if (object.getX() == 2390 && object.getY() == 4718) {
        if (player.getX() != 2390 || player.getY() != 4719) {
            return;
        }
        player.setAnimation(840);
        var toTile = new Tile(2390, 4717, 0);
        player.setForceMovement(new ForceMovement(player, 0, toTile, 2, Tile.SOUTH));
        player.lock();
        player.getGameEncoder().sendMessage("You start to climb the precarious rocks.");
        var event = new Event() {
            execute: function() {
                event.stop();
                if (!player.isVisible()) {
                    return;
                }
                player.unlock();
                player.getMovement().teleport(toTile);
                player.getGameEncoder().sendMessage("You climb confidently over the rocks and hold your balance well.");
            }
        };
        player.getWorld().addEvent(event);
    }
}

// Mossy rock
DecodedPacketObjectOption.prototype.object_2965 = function(player, index, object) {
    player.getGameEncoder().sendMessage("These rocks look somehow manufactured.");
}

// Furnace
DecodedPacketObjectOption.prototype.object_2966 = function(player, index, object) {
    player.getGameEncoder().sendMessage("Perhaps I should use this to forge something...");
}

// Recess
DecodedPacketObjectOption.prototype.object_2969 = function(player, index, object) {
    if (player.getCombat().getLegendsQuest() >= 2) {
        player.getGameEncoder().sendMessage("The wall appears to glow from the crystal.");
        return;
    } else if (player.getInventory().getCount(745) < 1) {
        player.getGameEncoder().sendMessage("It looks like something needs to be placed into the wall.");
        return;
    }
    player.getCombat().setLegendsQuest(2);
    player.getInventory().deleteItem(745, 1);
    player.getGameEncoder().sendMessage("You carefully place the crystal into the depression.");
}

// Filled Recess
DecodedPacketObjectOption.prototype.object_2970 = function(player, index, object) { }

// Shimmering field
DecodedPacketObjectOption.prototype.object_2971 = function(player, index, object) {
    if (player.getCombat().getLegendsQuest() < 2) {
        player.getGameEncoder().sendMessage("You try to pass through the field, but something prevents you.");
        return;
    }
    if (player.getY() > object.getY()) {
        player.getMovement().teleport(player.getX(), object.getY());
    } else {
        player.getMovement().teleport(player.getX(), object.getY() + 1);
    }
}

// Boulder
DecodedPacketObjectOption.prototype.object_2972 = function(player, index, object) { }

// Boulder
DecodedPacketObjectOption.prototype.object_2973 = function(player, index, object) { }

// Boulder
DecodedPacketObjectOption.prototype.object_2974 = function(player, index, object) { }

// Leafy Palm Tree
DecodedPacketObjectOption.prototype.object_2975 = function(player, index, object) { }

// Vine
DecodedPacketObjectOption.prototype.object_2989 = function(player, index, object) { }

// Vine
DecodedPacketObjectOption.prototype.object_2990 = function(player, index, object) { }

// Vine
DecodedPacketObjectOption.prototype.object_2991 = function(player, index, object) { }

// Vine
DecodedPacketObjectOption.prototype.object_2992 = function(player, index, object) { }

// Vine
DecodedPacketObjectOption.prototype.object_2993 = function(player, index, object) { }

// Vine
DecodedPacketObjectOption.prototype.object_2994 = function(player, index, object) { }

// Closed chest
DecodedPacketObjectOption.prototype.object_2995 = function(player, index, object) { }

// Closed chest
DecodedPacketObjectOption.prototype.object_2996 = function(player, index, object) { }

// Arctic Pine
DecodedPacketObjectOption.prototype.object_3037 = function(player, index, object) { }
