{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Core as Lib",
            "type": "shell",
            "command": "cd bin;rm ../data/lib/core.jar;zip -r ../data/lib/core.jar com -i \"*.class\";cd ../data/lib; java -cp emptymethods.jar Main core.jar core.jar"
        }
    ]
}
